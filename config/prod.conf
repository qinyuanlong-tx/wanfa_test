{
    "global": {
        "JAVA_PORT" : "7001",
        "G_SERVER"  : "http://g.tbcdn.cn/"
    },
    "logger" : {
        // 上线前，可依照PE要求变更logger设定
        "stderr"      : false,      // 是否顯示error stack在命令行
        "level"       : "INFO",     // 輸出到文件的最低級別為INFO
        "stdoutLevel" : "INFO"      // 輸出到命令行的最低級別，默認不輸出
    }
}
